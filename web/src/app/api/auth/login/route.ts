import { NextResponse } from 'next/server'; import { createClient } from '@/utils/supabase/server'; export async function POST(request: Request) { const formData = await request.formData(); const email = formData.get('email') as string; const password = formData.get('password') as string; const supabase = await createClient(); const { data: authData, error } = await supabase.auth.signInWithPassword({ email, password }); if (error) { return NextResponse.redirect(new URL('/login?error=Invalid login credentials', request.url), 303); } if (authData.session) { const html = \<html><head><meta http-equiv=\"refresh\" content=\"0;url=/dashboard\"></head><body style=\"background:#09090b;color:#fafafa;display:flex;align-items:center;justify-content:center;height:100vh;font-family:sans-serif;\"><h2>Login Successful! Redirecting...</h2><script>window.location.href = '/dashboard';</script></body></html>\; const response = new NextResponse(html, { status: 200, headers: { 'Content-Type': 'text/html' } }); response.cookies.set('grayjay-api-token', authData.session.access_token, { secure: true, sameSite: 'none', path: '/', maxAge: 60 * 60 * 24 * 365 }); return response; } return NextResponse.redirect(new URL('/login?error=Unknown error', request.url), 303); }
