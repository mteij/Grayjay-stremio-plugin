import { NextResponse } from 'next/server'; import { createClient } from '@/utils/supabase/server'; export async function GET(request: Request) { const supabase = await createClient(); const { data: { session } } = await supabase.auth.getSession(); const response = NextResponse.redirect(new URL('/dashboard', request.url), 303); if (session) { response.headers.set('grayjay-api-token', session.access_token); } return response; }
